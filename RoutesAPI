
// constructor
public Routes(String filename, init initPop)

// accessor methods
public int getAlive()
public int getDead()
public int getEscaped()
public int getPop()

// draws intersections, roads
public void draw()

// distribute pseudorandom flow across flow network
public void populate(int population)

// distance from detonation to point on plane
public double detDist(Point p)

// probability of a smart choice being made re:direction
public double awareness(Point p)

// updates flow network
public void nextState()

// updates flow at a single intersection
public void update(int i, ST<Integer, Double> randoms, FlowNetwork f)

public String routesToString()

public String roadNetworkToString()

public EdgeWeightedDigraph roadGraph()

public FlowNetwork roadNetwork()

// set the explosion's current radius
public void setHazardRadius(double r)

// set the greatest distance an explosion can go? why does this exist?
// currently at 2/3 distance of farthest intersection
public double hazardLimit()

// is point on map within hazard radius?
public boolean isVulnerable(Point p)

// test method
public static void main(String[] args)

